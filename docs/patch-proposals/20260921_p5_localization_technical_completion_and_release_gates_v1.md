# P5 localization: technical completion checkpoint (21 Sep 2026)

**Status: candidate QA complete; academic approval and production correction NOT complete.** This file is public: never insert protected question stems, option arrays, answers, worked solutions, reviewer-only materials, user identifiers, or private tokens. Related #137, #139, #126 and PR #138.

## Confirmed read-only baseline

- Audited 252 unique P5 questions in published assessment memberships. A scoped audit identifies **17 unique affected questions**: 10 original Representation items with English prose choices on RU/UZ screens; seven NOR-06 items with unlocalized normal-approximation terminology and ambiguous comma-separated binomial notation in RU stems/options/explanation. The 10 and seven sets do not overlap. Numerical and purely symbolic option arrays are valid negative controls and must not be counted as translation defects solely because their strings match EN.
- All 17 production question snapshots match their private content-meta MD5 before remediation. All 17 legacy `public.questions` items are `is_active=false`, `quality_status=draft`, and must remain isolated from Practice and Tours. The 17 questions have 24 assessment memberships in total; four of the first ten also occur in later assessments. No direct target-question references in Practice/Tours answers/pools or legacy Exam Prep question-skill references were found in the read-only checks.
- Exactly **one** frozen, finalized P5 diagnostic item (question ID 6253) has one saved test response, one evidence event and an associated correction action. The existing evidence references its **original exact question hash**; a simple content UPDATE plus metadata-hash rewrite would break the frozen session/evidence contract. The other 16 target questions have no frozen session items as of this checkpoint. The user confirmed the current three Core beta accounts are their test accounts, but gave no blanket authority to delete other app data or silently rewrite evidence. The P5 cohort must be rechecked immediately before any release.

## Private content candidate and mathematical precheck

- Candidate payloads for all 17 questions are prepared **outside the public repository**. Ten sets of RU/UZ answer choices preserve all four positions (80 localized choice strings). One originally ambiguous mixed item has a newly clarified EN/RU/UZ stem separating the numerical mean calculation from a subsequent median/IQR visual comparison; this is a substantive wording clarification and needs explicit independent review. Seven NOR-06 items have corrected RU/UZ stems and relevant explanations/options with unambiguous two-parameter binomial notation.
- A private local invariant checker passed: 17 distinct targets, 80 nonempty translated option strings, no remaining verbatim English prose options in the ten candidates, no flagged English `normal approximation` / `continuity correction` in the seven candidate stems, no ambiguous `Bin(n,0,d)` formatting in their proposed stems/options. Five displayed normal-approximation values were separately recalculated with independent numeric functions; representation arithmetic was checked. These are **assistant checks, not independent academic sign-off**. The private packet has empty reviewer/date fields, rather than fabricated approvals.
- Public synthetic Node regression now checks copied English prose choices, mathematical-only choice exclusions, extra-comma Bin notation and untranslated terminology in **stems AND explanations**. This is a detector and not a production publishing gate or a proof that any specific published question is fixed.

## Release-blocking decisions — no silent shortcuts

1. Qualified independent mathematics and RU/UZ language review of all 17 actual protected candidates, including answer uniqueness, every distractor, four-choice order, corrected mixed stem, continuity-correction maths, notation, 3-language mobile rendering and explanation parity. Obtain real reviewer identity, dated sign-off and evidence reference. Do not set any `approved_at` or QA pass on AI assertions.
2. Independently resolve #126: actual human sign-off for the EXACT compositions of eight original published assessment sets and any proposed changes. Previously populated item QA flags or null assessment approval dates do not prove this.
3. Avoid in-place mutation of history-bearing ID 6253 unless a separate specifically authorized test-only evidence cleanup has passed a complete FK/progress/audit impact check; never revise a finalized frozen session hash or evidence source version to conceal content drift. A versioned successor with independently versioned assessment and routing is the history-preserving alternative. Treat downstream memberships, assessment versions, diagnostic rule bindings and holdout confidentiality as one atomic reviewed package.
4. Recheck active sessions at release time; pause only the relevant P5 target flow with explicit scoped approval if necessary, not the global Core. Require immutable baseline hashes, exact affected membership preconditions, disposable PostgreSQL positive/negative tests, rollback and RU/UZ/EN UI checks. Obtain separate explicit owner authorization for each live SQL migration, merge/deploy, flag or enrollment change. Do not mutate original migration files, Practice/Tours/certificates/ratings or unrelated users.
5. #136 written rubric localization is a separate outstanding issue: fixing these 17 MCQ items alone does not complete the entire trilingual written-assessment QA or justify a blanket P5 completion claim.

**This checkpoint does not authorize executing SQL, deleting any test account/session, or declaring P5 publication academically approved.**
