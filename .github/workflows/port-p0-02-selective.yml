name: Port P0-02 selective security release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - name: Release port is frozen
        run: |
          echo "Selective P0-02 port is complete and frozen."
          echo "Do not rerun cherry-picks; validate the release branch with regression gates instead."
